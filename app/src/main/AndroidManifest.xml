<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.etsisi.appquitectura">

    <application
        android:name="com.etsisi.appquitectura.application.BaseApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Splash"
        android:allowBackup="false">
        <activity
            android:name="com.etsisi.appquitectura.presentation.ui.login.view.LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/LoginActivityStyle"
            android:screenOrientation="nosensor"
            android:noHistory="true"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:host="etsisi.page.link"
                    android:scheme="https"/>
                <data
                    android:host="etsisi.page.link"
                    android:scheme="http"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.etsisi.appquitectura.presentation.ui.main.view.MainActivity"
            android:launchMode="singleTask"
            android:screenOrientation="nosensor"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
            </intent-filter>
        </activity>
    </application>

</manifest>